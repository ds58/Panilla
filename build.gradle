allprojects {
    group = 'com.ruinscraft'
    version = '1.4.8-fix'
}

subprojects {
    apply plugin: 'java'

    repositories {
        mavenLocal()
        jcenter()
    }

    sourceCompatibility = '1.8'
}

task clean {
    delete './target'
}
