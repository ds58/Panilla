allprojects {
    group = 'com.ruinscraft'
    version = '1.6.4-icleanersync'
}

subprojects {
    apply plugin: 'java'

    repositories {
        mavenLocal()
        jcenter()
    }

    sourceCompatibility = '1.8'
}

task clean {
    delete './target'
}
